pipeline{ 
    agent any

    stages { 
        
        stage("build") {
            steps {
                sh 'echo hihi'
            }
        }

        stage("test") {
            steps {
                sh 'echo testest'
            }
        }

        stage("deploy") {
            steps {
                sh 'echo deployy'
            }
        }
    }
}